var vm = new Vue({
    el: '#app',
    data: {
        isActive: true,

        classObject: {
            active: true
            // has-error: function () {}
        },

        activeClass: 'active'
    }
});